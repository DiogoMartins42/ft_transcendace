export let gameSettings: {difficulty: number, resetSpeed: boolean, 
	multiplayer: boolean, mouse: boolean, paddleSpeed: number, ballSpeed: number} =
{
	difficulty: 0.1,
	resetSpeed: true,
	multiplayer: true,
	mouse: false,
	paddleSpeed: 5, // pixels per frame
	ballSpeed: 3,
}